import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  turbopack: {
    // Optional: you can safely omit this, Turbopack is enabled by default
  },
  // You can add other config here, like React strict mode or images
};

export default nextConfig;
